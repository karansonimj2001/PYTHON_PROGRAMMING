def mul(a,b): #parameters
    print(a*b)

mul((2+1-1),3)  #2,3 arguments
mul(4,5)  #4,5 args

x=int(input("num-1="))
y=int(input("num-2="))

mul(x,y)  #x,y args

#Parameters & Arguments

    #Parameters:
        #are variables that represent input data to function logic
        #we define parameters in function header inside ()
    #Arguments:
        #Value of a parameter is called argument
        #we pass arguments in function call inside ()
    
