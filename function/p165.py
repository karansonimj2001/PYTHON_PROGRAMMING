def mul(a,b):
    '''
    this function prints product of 2 numeric values
    error if values are not compatiable
    '''
    print(a*b)


help(mul) #it extract docstring(triple quoted string) from fun
